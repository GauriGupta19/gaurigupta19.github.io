# Project Setup

Minimal instructions to set up and run this codebase locally.

- Install dependencies:

```bash
npm install
```

- Run development server:

```bash
npm run dev
```

- Build for production:

```bash
npm run build
```

That's it — this README only contains the minimal setup steps for the repository.
